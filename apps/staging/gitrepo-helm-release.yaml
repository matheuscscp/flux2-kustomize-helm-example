apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: test-gitrepo
  namespace: flux-system
spec:
  releaseName: test-gitrepo
  chart:
    spec:
      chart: charts/flux-status-server-config
      sourceRef:
        kind: GitRepository
        name: flux-system
  interval: 10m
  values:
    config:
      foo: bar
    devConfig:
      baz: qux
